#set($previous = "")
#set($previous_ok = "")
#set($next_ok = "")
#set($encontrado = false)

#if($index)
	#foreach($node in $index.getChilds())
		#if(${node.name} == "link")
			#if(${doc_name}==${node.textSufix})
				#set($encontrado = true)
				#set($previous_ok = $previous)
			#else
				#if($encontrado && $next_ok == "")
					#set($next_ok = $node)
				#end
			#end
			#set($previous = $node)
		#end
	#end

	<div class="column row book_nav clearfix">
		#if($previous_ok != "")
           	<a style="float:left;"	href="${previous_ok.textSufix}" title="${previous_ok.textCentral}">&#x25C4; ${lang.get("nav.prev")}</a>
		#end
		#if($next_ok != "")
           	<a style="float:right;" href="${next_ok.textSufix}" title="${next_ok.textCentral}">${lang.get("nav.next")} &#x25BA;</a>
		#end
	</div>
#end						
