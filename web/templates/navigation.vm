#if(${doc.getChild("navigation")})
	<div class="column row">
    <ul class="menu book_nav">
		#set($nav = ${doc.getChild("navigation")})

		#if($nav.getChild("previous"))
			<li style="float:left;">
            	<a 	href="chapter_${nav.getChild("previous").tvalue}.html" title="${lang.get("nav.prev")}">&#x25C4; ${lang.get("nav.prev")}</a>
			</li>
		#end
		#if($nav.getChild("next"))
			<li style="float:right;">
            	<a href="chapter_${nav.getChild("next").tvalue}.html" title="${lang.get("nav.next")}">${lang.get("nav.next")} &#x25BA;</a>
			</li>
		#end
	</ul>
	</div>
#end						
